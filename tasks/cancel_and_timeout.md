# cancel_and_timeout — Implement /cancel command and 5-minute timeout mechanism to abort incomplete setups and return to Main Menu

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 TONBOT

Implement /cancel command and 5-minute timeout mechanism to abort incomplete setups and return to Main Menu